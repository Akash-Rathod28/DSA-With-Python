class Solution:
    def minPartitions(self, n: str) -> int:
        return int(max(n))
        # a = 0
        # for i in n:
        #     if int(i) > a:
        #         a = int(i)
        # return int(a)
